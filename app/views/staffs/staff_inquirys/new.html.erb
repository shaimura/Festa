<%= form_for @staff_inquiry, url:staffs_staff_inquirys_path do |inquiry| %>

<div>
	<%= @staff.name %>
</div>

 <div class="form-group">
   <%= inquiry.label :message, 'お問い合わせ内容' %>
   <%= inquiry.text_area :message, size: '10x10', class: 'form-control' %>
 </div>

  <%= inquiry.submit '送信' %>


<% end %>