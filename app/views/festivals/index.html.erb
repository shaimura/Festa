

<!-- 検索結果 -->
<% @search.each do |festival| %>

	<%= festival.name %>
	<%= attachment_image_tag festival,:fes_image, :fill,300,300 %>

<% end %>


<!-- 検索結果が０件ならば -->
<% if Festival.name.present? %>

	<% @festivals.each do |f| %>

	<%= f.name %>
	<%= attachment_image_tag f,:fes_image, :fill,300,300 %>

	<% end %>

<% end %>